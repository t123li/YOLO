# track_3class_window_bind.py 修改记录

_对象：`target tracking/track_3class_window_bind.py`（D 方案跟踪脚本）_

> 本文档记录本脚本从创建到现在的每一次修改详情，并标注每个版本在 12 个验证序列上的计数表现，方便定位最优基线版本。**最优基线的判断标准 = 准确计数**（预测与人工核实人数最接近）。

---

## 📌 版本总览

| 版本 | 输出目录 | 精确(0) | ±1 | ≥+2 | 最差 | 核心改动 |
| --- | --- | --- | --- | --- | --- | --- |
| v1 | 20260828_001 | — | — | — | — | 初版：车锚定 relx + 贪心窗匹配（窗 ID 不稳，弃） |
| v2 | 20260828_002~004 | 5 | 5 | 2 | +2 | 窗 ID 稳定（IoU/位移）+ 人窗内 rel 关联 |
| **v3 ✅ 正式版本** | 20260828_014 | **6** | **5** | **1** | +1 | 方向感知窗槽位 + pred 即时累计 + 首帧框窗 |
| v4（实验，回退） | 20260829_005 | 5 | 7 | 0 | -1 | 匈牙利 + 顺序保持 + 生命周期 + 座位槽 + 两级关联 |
| v5（实验，回退） | 20260829_006 | 5 | 5 | 2 | +2 | v4 + 单帧乘客不过滤（min_hits=1） |
| v6（实验，回退） | 20260829_009 | 4 | 7 | 1 | +2 | v5 + tracklet 合并（治 ID 分裂） |
| **v7 ✅ 最新最优** | 20260830_003/004 | **10** | **19** | **1** | +2 | V4_pending（待认领池+确认机制）+ 低分暂存（高分建 ID/低分暂存认领） |

> **✅ 正式版本 = v3（20260828_014，重跑确认 20260829_010）**：精确 6、±1 有 5、≥+2 有 1，无欠计数>1、无过计数>1，准确计数最佳。
> **v4/v5/v6 均未达 v3 基线，已回退。** 教训：v3 的"简单 = 每个检测都算数"（`CONF_HEAD=0.15` + `pred=len(gid_hits)`）在计数上是对的；后续"抑制性优化"（低 conf 多检 + 生命周期过滤 + 座位槽 + 匈牙利 + tracklet 合并）净效果为负——要么引入欠计数（过滤单帧），要么引入过计数（低分碎片/误合并）。
> v4/v5/v6 牺牲了部分准确计数换取"防过计数"和"ID 稳定"，若以纯计数准确率为准，**v3 仍是最优基线**。

---

## 详细修改历史

### v1（创建，commit `e93335c`）

- **内容**：D 方案首版，3 类检测 + 车窗跨帧跟踪 + 运动补偿&OSNet 人跟踪 + 窗口绑定。
- **车窗跟踪**：用"车锚定 relx"（窗口中心相对车框归一化 x）做跨帧匹配。
- **结果**：窗 ID 严重不稳定（relx 依赖车框，车框噪声 → relx 漂移 → 窗 ID 重建 → 人绑定全乱），单序列 21 差 15，废弃。

### v2（调试迭代，commit `e93335c` 后未单独提交）

- **窗 ID 稳定**：改"中位数位移补偿 + IoU"匹配，窗 ID 稳定（f0-f6 全程 [0,1,2]）。
- **人关联**：加窗内 rel 坐标（同窗用 rel 距离、异窗用全局像素距离），解决透视投影下 RANSAC 失效。
- **结果**：12 序列精确 5、±1 有 5、≥+2 有 2（最差 +2）。对应 `tracking_output/20260828_002~004`。

### v3（commit `cac6a8c`，**最优计数基线**）

- **车窗 ID 物理稳定**：先判断车头方向（首帧贴边启发式 + 位移确认），用"离车头槽位"匹配，前窗=槽0，中间插入新窗不影响。`20260714180408088` 前/中/后窗 ID 全程稳定。
- **首帧框窗**：首帧用启发式方向立即建窗。
- **每窗人数实时**：显示本帧落在窗内的检测数（非历史累计）。
- **pred 即时累计**：`pred = len(gid_hits)`（所有出现过的 ID），出现即计数。
- **结果**：`tracking_output/20260828_014`，精确 **6**、±1 有 5、≥+2 有 1（+1）。**准确计数最佳。**

### v4（commit `6882cf4`）

依据《车辆超员场景_跟踪计数问题分析与改进方案_20260828.md》做六项改进，针对三类问题：

| 问题 | 改进 |
| --- | --- |
| ID 跳变 | ① 匈牙利全局匹配（替贪心）② 窗内顺序保持（rel_x 差>0.35 窗宽禁止）③ 修复 OSNet BGR→RGB |
| 过计数 | ④ 生命周期 Tentative→Confirmed（min_hits=2）⑤ 座位槽上限（SEATS_PER_WINDOW=3） |
| 欠计数 | ⑥ 两级关联（predict conf=0.05 多检，高分≥0.35 可建轨迹，低分只延续） |

- **结果**：`tracking_output/20260829_005`，精确 5、±1 有 7、≥+2 有 **0**（无过计数）。ID 跳变大幅减少（`20260714180408088` gid 全程稳定）。
- **代价**：3 个序列 0→-1（生命周期把"只露 1 帧"的真乘客过滤了）。

### v5（当前，未提交）

- **单帧乘客不过滤**：用户反馈"只露 1 帧的乘客不能过滤"，`confirmed_ids` 改为包含所有出现过的 ID（`min_hits=1`），`total_count = len(gid_hits)`。欠计数是主要矛盾。
- **结果**：`tracking_output/20260829_006`，精确 5、±1 有 5、≥+2 有 2（+2, +1, +1）。
- **改善**：`20260715134221208` 从 v4 的 0 → +1（欠计数 2 人救回）；`20260715071110945`/`20260715132934340` 保住精确。
- **代价**：`20260715081444132` +1→+2、`20260715175447965` 0→+1（单帧误检/ID 碎片被计入，过计数回升）。

### v6（当前，未提交）

- **tracklet 合并（治 ID 分裂）**：v5 的 +2 过计数来自 ID 分裂（同一人跨帧关联失败被给两个 ID）。序列结束后离线合并：同窗 + 时间相邻（重叠≤MERGE_MAX_GAP）+ 窗内 rel_x 接近（<0.12）+ 外观相似（>0.80）的 tracklet 用并查集合并。
- **实现**：`MotionCoherentTracker` 新增 `tracklet_info`（每 gid 记录帧/rel_x/外观特征）和 `merge_tracklets()`；`process_sequence` 末尾调用，用合并后计数。
- **结果**：`tracking_output/20260829_009`，精确 4、±1 有 7、≥+2 有 1（+2）。
- **改善**：`20260715071110945` -1→0、`20260715132934340` +1→0（误合并修复 + 合并有效）。
- **局限**：`20260715081444132` 仍 +2——其分裂根因是**检测框位置不稳定**（同一人 rel_x 从 0.61 漂到 0.22，透视/抖动），非简单时间断裂，位置类合并判据覆盖不了。

### v7（当前，`track_3class_window_bind_v4_pending.py` + 低分暂存）

**背景**：2026-08-30 建立检测器评估体系（`evaluate_detector.py`，val/test 全量逐帧 IoU 匹配），发现 head 检测漏检率极低（0.6%）但误检率高（62%，低置信杂框）。跟踪层据此加"分级用检测"：高分建身份、低分暂存待认领。

**改动（`MotionCoherentTracker` 内新增，V4_pending 基础上）**：
1. **低分暂存池**（`lowconf_pool` + `lowconf_gids`）：head 检测 `conf < CONF_NEW_ID(0.25)` 且未匹配到已有轨迹时，不再新建正式 ID，而是分配临时 gid 存入暂存池（记录特征/绑定窗/帧），**不建轨迹、不计数**。
2. **高分认领暂存**：阶段二回溯时，高分检测与暂存池特征匹配（`sim > claim_thr` 且窗口绑定一致）→ 认领回同一身份，升级为正式 gid（防"遮挡/模糊导致某帧低分 → 被当新 ID"的碎片）。
3. **暂存老化**：`LOWCONF_AGE=5` 帧未被认领则丢弃（噪声自然过期，不累积）。
4. **计数排除**：`confirmed_ids()` 排除 `lowconf_gids`（低分暂存身份永不计入）。

**结果（当前标注，20 序列，A/B 对比）**：
- `tracking_output-标注人数版本/20260830_003`（CONF_NEW_ID=0.25）、`20260830_004`（CONF_NEW_ID=0 对照）。
- A/B 两版逐序列完全一致 → 本批序列 head 检测基本都 ≥0.25，低分暂存**未触发**（该批无低分框），改动为"无害保留"的保护机制。
- **V7 当前标注 20 序列：精确 10/20、±1 有 19、≥+2 有 1、MAE 0.55**（V3 同口径：精确 9、MAE 0.60）→ **V7 略优于 V3**。
- 逐序列 V7 优于 V3 4 个（含 `20260729134303548` +2→0）、劣于 4 个、平 12 个。
- 剩余 +2：`20260715175447965`（pred 8 vs actual 6）——该序列 head 全高分，低分暂存不触发，+2 来自高分 ID 分裂（V4 原有）。

---

## 配置对照（v3 → v7）

| 配置 | v3 | v4 | v5 | v6 | v7 |
| --- | --- | --- | --- | --- | --- |
| `CONF_HEAD` | 0.15 | 0.10 | 0.10 | 0.10 | 0.15 |
| `CONF_HEAD_HIGH` | — | 0.35 | 0.35 | 0.35 | — |
| predict conf | 0.2 | 0.05 | 0.05 | 0.05 | 0.2 |
| 匹配 | 贪心 | 匈牙利 | 匈牙利 | 匈牙利 | 贪心（V4_pending 基础） |
| 窗内顺序保持 | — | 有 | 有 | 有 | 有（WIN_REL_GATE） |
| 生命周期 min_hits | 全部计数 | 2 | **1（单帧不过滤）** | 1 | 确认机制（进窗+hits≥2/高conf） |
| 座位槽上限 | — | 3 | 3 | 3 | — |
| 计数 | `len(gid_hits)` | confirmed(≥2帧) | `len(gid_hits)` | `len(gid_hits)` | `len(confirmed_ids)` 进窗确认 |
| tracklet 合并 | — | — | — | **有** | — |
| 待认领池 | — | — | — | — | 有（V4 长期身份记忆） |
| 低分暂存 | — | — | — | — | **有（CONF_NEW_ID=0.25）** |
| OSNet 输入 | BGR | RGB | RGB | RGB |
| 计数 | `len(gid_hits)` | confirmed(≥2帧) | `len(gid_hits)` | `len(gid_hits)` |
| tracklet 合并 | — | — | — | **有** |

---

## 最优基线选择

**判断标准：准确计数（预测 vs 人工核实人数），同数据/同标注/同序列集对比。**

- **v7（20260830_003/004，`track_3class_window_bind_v4_pending.py` + 低分暂存）= 当前最新最优**：当前标注 20 序列精确 **10/20**、±1 有 19、≥+2 有 1、MAE **0.55**。同口径 V3 = 精确 9、MAE 0.60。**v7 略优于 v3。**
- **v3（20260828_014，重跑确认 20260829_010）= 上一正式版本**：旧 12 序列精确 6，仍可靠。
- **v4/v5/v6 = 早期实验，已回退**。

**选型建议**：
- **当前采用 v7**（V4_pending + 低分暂存）：精确 10/20、MAE 0.55，同口径最优。
- v7 的"低分暂存"在本批序列未触发（head 全高分），是保留的保护机制；真正带来增益的是 V4 的确认机制 + 待认领池。
- **剩余误差**：仅 1 个 ≥±2（`20260715175447965` +2，高分 ID 分裂）+ 若干 ±1。欠计数（-1）来自检测召回（遮挡头未检出），过计数（+1）来自少量 ID 碎片。

**v7 的剩余误差分布**（当前标注）：`0 -1 0 0 2 1 0 0 -1 -1 0 -1 -1 1 0 0 0 -1 -1 0`。
